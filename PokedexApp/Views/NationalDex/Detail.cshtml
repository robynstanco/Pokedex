@using Pokedex.Common;

@model PokemonDetailViewModel

@{
    ViewData["Title"] = Constants.NationalDex + " - " + Model.Name;
}

<div class="pokemon__container">
    <div class="pokemon__item--large">
        <h1>@Model.Name</h1>
    </div>
</div>